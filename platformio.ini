[platformio]
src_dir = .
lib_dir = library

[env:esp32-c6-devkitc-1]
platform = espressif32
board = esp32-c6-devkitc-1
framework = arduino
monitor_speed = 115200
build_flags = 
    -DCORE_DEBUG_LEVEL=0
lib_deps = 
    ${common.lib_deps}

[common]
lib_deps =
    # No external dependencies - using local library
